apply plugin: 'java'

sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile project (':commonlib')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}